# Sao chép thành `.env.local` để chỉnh khi dev (file đó không commit).
# Production: chủ yếu dùng `.env.production` + `next build`.

NEXT_PUBLIC_SITE_URL=https://kytucxasinhvien.vn
NEXT_PUBLIC_GOOGLE_SHEET_WEBHOOK_URL=
